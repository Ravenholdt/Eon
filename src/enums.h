#pragma once

enum class attributes { strength, endurance, mobility, personality, psyche, willpower, education, vision, hearing, smell, max };
enum class personalities {loyalty, honor, amor, aggression, faith, generosity, fame, luck, max};

enum class skills {sword, dodge, max};

enum class damageTypes {pain, trauma, blood, bleed, max};